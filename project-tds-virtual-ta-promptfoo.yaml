providers:
  - id: my-ta-api
    label: "Virtual TA API"
    config:
      type: api
      url: "https://tds-virtual-ta-zhsd.onrender.com/api/"

scenarios:
  - name: "Token cost with Japanese text"
    config:
      provider: my-ta-api
    requests:
      - prompt: "How much would the input token cost for gpt-3.5-turbo-0125 if I use the Japanese text?"
        assert:
          - type: contains
            value: "cost"

  - name: "Fallback for unclear question"
    config:
      provider: my-ta-api
    requests:
      - prompt: "What's the square root of banana?"
        assert:
          - type: contains
            value: "couldn’t identify"